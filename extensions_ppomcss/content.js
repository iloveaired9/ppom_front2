// Content script for CSS Audit
// Currently used for background monitoring or future advanced features

console.log('PpomCSS Audit content script loaded.');

// Future: Highlight used elements on the page
// chrome.runtime.onMessage.addListener((request, sender, sendResponse) => {
//   if (request.action === 'highlightUsed') {
//     // Implementation for highlighting
//   }
// });
